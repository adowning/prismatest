# Migration `20201125235433-fourth`

This migration has been generated by adowning at 11/25/2020, 5:54:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "blog"."Comment" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"authorId" text   ,
"workorderId" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "blog"."Workorder" (
"id" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
"imported" boolean   ,
"orderID" text   NOT NULL ,
"accountID" text   NOT NULL ,
"accountName" text   NOT NULL ,
"address1" text   ,
"refNumber" text   ,
"city" text   ,
"state" text   ,
"zip" integer   ,
"companyID" text   ,
"correction" integer   ,
"createdBy" text   ,
"createdOn" timestamp(3)   ,
"dateCreated" timestamp(3)   ,
"balanceDue" integer   ,
"grandTotal" integer   ,
"orderNumber" integer   NOT NULL ,
"orderType" "OrderType"  ,
"originalType" "OrderType"  ,
"postToAccounting" boolean   ,
"salesRepID" text   ,
"siteID" text   ,
"subTotal" integer   ,
"taxID" text   ,
"taxRate" integer   ,
"taxTotal" integer   ,
"userName" text   ,
"merchantFeesTotal" integer   ,
PRIMARY KEY ("id")
)

ALTER TABLE "blog"."Comment" ADD FOREIGN KEY ("authorId")REFERENCES "blog"."User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "blog"."Comment" ADD FOREIGN KEY ("workorderId")REFERENCES "blog"."Workorder"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201125214210-third..20201125235433-fourth
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -12,17 +12,18 @@
   provider = "prisma-dbml-generator"
 }
 model User {
-  id        String   @id @default(cuid())
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  email     String   @unique
+  id        String    @id @default(cuid())
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  email     String    @unique
   password  String
   firstname String?
   lastname  String?
   posts     Post[]
   role      Role
+  Comment   Comment[]
 }
 model Post {
   id        String   @id @default(cuid())
@@ -41,8 +42,18 @@
   VOIDED
   WORKORER
 }
+model Comment {
+  id        String   @id @default(cuid())
+  createdAt DateTime @default(now())
+  author    User?    @relation(fields: [authorId], references: [id])
+  authorId  String?
+
+  Workorder   Workorder? @relation(fields: [workorderId], references: [id])
+  workorderId String?
+}
+
 model Workorder {
   id        String   @id @default(cuid())
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
@@ -74,8 +85,10 @@
   taxRate           Int?
   taxTotal          Int?
   userName          String?
   merchantFeesTotal Int?
+  comments          Comment[]
+
 }
 enum Role {
   ADMIN
```


